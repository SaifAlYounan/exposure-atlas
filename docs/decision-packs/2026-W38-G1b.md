# G1b decision pack — week 2026-W38 (immutable manifest)

Created 2026-08-31 by the builder coordinator under A0, in response to
decision D-019 (design a GitHub-hosted confinement qualification for
HAR-001/HAR-003). Answer in `2026-W38-G1b-answers.yaml`. Est. ≈45 min.
A1 (D-021) remains ineffective until this pack approves the design AND
the qualification passes.

## What the builder built (A0; all fixture/config-level, nothing live)
- **ADR-0002** — the honest engineering verdict: HAR-001/HAR-003 CAN be
  qualified on GitHub-hosted infrastructure, conditional on four
  operator-only controls (R1). Residuals R1–R3 are stated explicitly,
  not diluted.
- **`.github/workflows/confinement.yml`** — runs now under A0: proves
  private/metadata egress is blocked in-code, runs the SDK-denial
  fixture set against the host policy, uses harden-runner (audit mode).
- **`.github/CODEOWNERS`** — makes workflows + source/rights config
  operator-owned.
- **`.github/workflows/live-fetch.yml.template`** — the A1 fetch
  workflow, saved inert (`.template`) so it cannot trigger; harden-runner
  `block` mode with the exact pilot-host allowlist; targets a protected
  `live-fetch` environment; enforces the D-021 caps.
- **`tests/test_confinement.py`** — 4 confinement fixtures green;
  guards that the ADR residuals cannot be silently dropped.

## The honest residual you must decide on (ADR-0002 R1)
"The active agent cannot modify host policy" is only closed by controls
the builder must NOT hold and cannot set itself. These are yours:

| Item | Question | Proposed | Est. min |
|---|---|---|---|
| G1b-Q1 | Approve ADR-0002's design and verdict as the HAR-001/HAR-003 qualification approach? | Approve | 15 |
| G1b-Q2 | Will you set the four R1 controls before A1 activation: (1) branch protection on `main` requiring PR review; (2) CODEOWNERS enforcement on `.github/**`, `config/sources/**`, `config/rights/**`, `config/source-policy/**`; (3) a protected `live-fetch` GitHub Environment with you as required reviewer, holding any source credential; (4) live-fetch triggers only from `main`/gated dispatch? | Operator to confirm each | 20 |
| G1b-Q3 | Confirm the CourtListener/RECAP terms permit automated read-only API access at ≤50 docs and conservative rate for internal evaluation (D-021 scope); FTC pages are US-gov works | Operator to confirm | 5 |
| G1b-Q4 | On approval, the builder implements SRC-004-80 (the A1 probe entrypoint) behind the inert template and returns for a final "activate" confirmation before anything fetches. Acceptable? | Yes | 5 |

If you decline any R1 control, ADR-0002 says plainly that A1 live
fetching cannot be honestly authorized on this infrastructure; the
builder will hold rather than dilute the requirement.
