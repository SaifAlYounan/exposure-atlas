# G1 decision pack — week 2026-W37 (immutable manifest)

Created 2026-08-31 by the builder coordinator under A0 (D-002/D-017).
Answer by filling `2026-W37-G1-answers.yaml` in one sitting.
Estimated review: ≈75 minutes. Restrictive defaults hold on silence.

## Evidence summary (all fixture-level; nothing live)
- 5 green clean-checkout CI runs (33425429841 → 33429288604), 157 tests.
- Task receipts in `plan/evidence.jsonl`; gate manifest
  `plan/gate-manifests/G1.json`; capability report
  `builder/conformance/claude-capability-report.json`.
- Working under fixtures: full no-AI slice (fetch→canonicalize→anchor→
  assert→verify→operator-approve→policy→two-build identical release→
  serve with suppression precedence); PostgreSQL immutability/atomicity/
  job-queue; 62-schema catalogue; monitoring temporal invariant;
  adapter conformance kit; archive gate disabled-by-default; budget/
  completion/role-policy harness controls; SEC-01..09-class fixtures.

## What still blocks a full G1 pass (gate-verify is honest about this)
1. **HAR-001** (sandbox confinement) and **HAR-003** (pinned SDK adapter
   qualification): only provable in the operator-provisioned sandbox.
2. Confinement fixtures inside BOOT-060/070 are recorded as NOT CLAIMED
   with substitution rows.
3. PLT-002 uses the filesystem evidence store + ephemeral PostgreSQL;
   MinIO and digest-pinned images await the sandbox stack.

| Item | Question | Proposed answer | Est. min |
|---|---|---|---|
| G1-Q1 | Accept the BOOT-060/070 capability report — 8 proven fixtures + 4 confinement fixtures deferred with substitutions — as sufficient **interim** G1 harness evidence, with full confinement qualification remaining a hard A1 precondition? | Accept as interim; A1 stays blocked on sandbox qualification | 20 |
| G1-Q2 | Sandbox provisioning (handoff §2): will you provision it, and roughly when? HAR-001/HAR-003 and the deferred fixtures close there | Operator to state | 10 |
| G1-Q3 | Accept the PLT-002 deviation (filesystem evidence store, ephemeral PostgreSQL, compose digests pinned at first sandbox pull) until the sandbox stack exists? | Accept with expiry = sandbox provisioning | 10 |
| G1-Q4 | Grant **A1** now, effective only once G1-Q1/Q2 close in the sandbox? Proposed scope: read-only probes of CourtListener API and ftc.gov listing/document pages only, ≤50 documents/source, conservative rate caps, recorded receipts, 30-day expiry, no runtime model calls on fetched documents (that is A2) | Grant contingent on sandbox qualification; otherwise defer | 20 |
| G1-Q5 | Acknowledge the kernel→PostgreSQL pipeline integration as the first G2 implementation step (G1 slice stays file-backed; DB-layer guarantees separately proven) | Acknowledge | 5 |
| G1-Q6 | Anything in the 5 CI runs / build-status you want re-derived or explained before G2 work starts? | none | 10 |
