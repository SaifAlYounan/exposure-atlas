# Repository inventory (FND-001)

As of 2026-08-31, base commit `4b1e287` (pre-build), inventory commit `29317e4028fcb1ac060810c78e8a5e9fd524d11c`.

## Applications, packages, scripts, workflows
None existed at base. The repository was seeded with `SPEC.md`,
`CLAUDE.md` and `docs/atlas-handoff.md` only. Everything else present
now was created by the A0 bootstrap (see git history; every commit names
its task IDs).

## Databases, storage, external integrations
None. No deployment exists; no production output exists; there are no
consumers of any current record schema (there are no records).

## Public exposure
`SaifAlYounan/exposure-atlas` on GitHub is the only repository. It
contains no source documents, no records, no secrets (full-history scan
performed on the first read-only turn: one seed commit, three text
files). Whether the repository is public or private is operator-visible
on GitHub; nothing in it is restricted material either way.

## Legacy merge/diff/apply scripts
None found — nothing to retain, wrap or replace.

## Record schema variants and consumers
None found. The expected 316-record legacy dataset is NOT in this
repository or its history (material difference M1; pack item G0-Q10).

## Sensitive material in history
None found (binaries: none; secrets: none; transcripts: none).
