# Security findings register

| ID | Finding | Severity | Status | Owner | Evidence |
|---|---|---|---|---|---|
| SEC-INFO-01 | Confinement egress-block VERIFIED in production: live-fetch run 33445968310 blocked `release-assets.githubusercontent.com` (setup-uv download) — an un-allowlisted host — proving harden-runner block mode enforces the pilot-host allowlist. No live fetch occurred (job died at dependency install). | informational (positive) | closed | builder | run 33445968310 job logs; ADR-0002 R3 |
| SRC-FIND-01 | CourtListener /opinion/<id>/ HTML view returns HTTP 202 with empty body to the probe; discovery (v4 search API) works but document acquisition must use the CL API opinion endpoint (JSON plain_text/html) or the storage.courtlistener.com PDF, with issuing-court corroboration (mirror custodian, SP-05). lead_id also needs the v4 result id field. | informational | closed — API acquisition path + lead_id-from-cluster-id implemented (PR #4/#5) and verified live: run 33471746597 shows lead_id now `cl-<clusterid>` and the probe calls `/api/rest/v4/opinions/?cluster=<id>`. Residual blocker moved to SRC-FIND-03. | builder | run 33448842013 → run 33471746597 probe-summary |
| SRC-FIND-02 | Probe run 33449903841 crashed before writing a summary (no artifact): query library queries were dated effective_from 2026-09-01 while the run clock was 2026-08-31, so QueryLibrary.active raised outside the guarded block. No fetch occurred. Fix: effective_from=2015-01-01 (boundary window start) + main() always emits a diagnosable summary. | informational | closed | builder | run 33449903841 (17s, 0 artifacts) |
| SRC-FIND-03 | CourtListener v4 **data** endpoint `/api/rest/v4/opinions/?cluster=<id>` returns HTTP 401 Unauthorized to an anonymous client (probe sends only a User-Agent, no Authorization header); all 10 candidate acquisitions failed on run 33471746597. The v4 **search** endpoint (`/api/rest/v4/search/`) still served anonymously (discovery succeeded). Full opinion-text acquisition from CL therefore requires an authenticated CL API token. Provisioning that credential into the confined `live-fetch` environment (a new GitHub Actions secret + `Authorization: Token …` header) is a **source-access/governance decision for the operator**, not a unilateral builder change — escalated, not auto-fixed. Confinement egress-block held throughout (positive). | informational | **closed — verified fixed (D-030).** PR #6 merged; authenticated run 33532399346 acquired + canonicalized 4/10 opinions (authenticated=true, HTTP 200, plain_text). Token wiring proven live; token never appears in the artifact. Residual burst-rate issue tracked separately as SRC-FIND-04. | builder | run 33471746597 (401) → run 33532399346 (200, authenticated) probe-summary |
| SRC-FIND-04 | CourtListener v4 API rate-limits back-to-back acquisition calls: authenticated run 33532399346 acquired 4/10 then received HTTP 429 Too Many Requests on the remaining 6. The probe fired opinion fetches with no inter-call pacing and did not honor `Retry-After`. Fix: a politeness delay between CL acquisition calls + honor the server's `Retry-After` on 429 with bounded backoff (within D-021 caps and the source registry's respect-published-API-limits constraint). Ordinary code change (no new capability/credential/egress host). | informational | fix in progress — pacing PR (probe honors Retry-After + inter-call delay) | builder | run 33532399346 probe-summary |
| (none open) | | | | | |

Adversarial-security-reviewer sessions append here. This register is
never edited destructively; findings are closed with disposition rows.
